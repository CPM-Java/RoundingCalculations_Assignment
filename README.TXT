public static String calcDistance(int x1,
                           int y1,
                           int x2,
                           int y2)
Computes the distance between two points
Parameters:
x1 - integer where (x1,y1) (x2,y2)
y1 - integer where (x1,y1) (x2,y2)
x2 - integer where (x1,y1) (x2,y2)
y2 - integer where (x1,y1) (x2,y2)
Returns:
The distance between the two points rounded to 3 decimal places as a String
calcQuadRoots



public static String calcSphere(double r)
Computes the volume and surface area of a sphere
Parameters:
r - double representing the radius of a sphere
Returns:
The volume and surface area rounded to 3 decimal places as a String